# == SWFT Compliance Authoring Engine Configuration ============================
# Copy this file to `.env` (kept out of version control) and replace the
# placeholder values with the endpoints and credentials for your Azure Database
# for PostgreSQL Flexible Server and Azure Blob Storage references.

# ----- Azure Database for PostgreSQL connection -----
# Fully qualified server host name (e.g., swft-db.postgres.database.azure.com)
SWFT_DB_HOST=your-postgres-hostname
# Logical database name that stores the compliance data.
SWFT_DB_NAME=swft
# Azure AD principal or database role that the CLI should connect as.
SWFT_DB_USER=principal@yourtenant.onmicrosoft.com
# Authentication mode: use `entra` for Microsoft Entra ID token auth (default)
# or `password` if you must fall back to traditional credentials.
SWFT_DB_AUTH=entra
# Required only if SWFT_DB_AUTH=password
SWFT_DB_PASSWORD=
# Scope for Azure AD token acquisition; keep default unless Microsoft updates it.
SWFT_DB_AAD_SCOPE=https://ossrdbms-aad.database.windows.net/.default
# Optional adjustments
SWFT_DB_PORT=5432
SWFT_DB_TIMEOUT=30

# ----- Local working directories (optional overrides) -----
# Base directory for CLI caches (defaults to ~/.swft)
SWFT_HOME=
# Override sub-directories if needed. Leave blank to accept defaults.
SWFT_STORE=
SWFT_PINNED=
SWFT_OUTPUTS=

